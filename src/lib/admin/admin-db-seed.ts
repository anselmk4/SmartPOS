/**
 * Deprecated: The SuperAdmin portal now operates directly on real Supabase PostgreSQL data via REST APIs.
 */
export async function seedAdminPlatformDataIfEmpty(): Promise<void> {
  // No-op: Supabase PostgreSQL is the single source of truth for Super Admin.
}
